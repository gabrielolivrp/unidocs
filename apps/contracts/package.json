{
  "name": "@unidocs/contracts",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "contracts:test": "npx hardhat test",
    "contracts:clean": "npx hardhat clean",
    "contracts:chain": "npx hardhat node",
    "contracts:compile": "npx clean && npx hardhat compile",
    "contracts:deploy": "npx hardhat run scripts/deploy.ts --network localhost",
    "contracts:testnet": "npx hardhat run scripts/deploy.ts --network sepolia"
  },
  "devDependencies": {
    "@unidocs/tsconfig": "*",
    "@types/node": "^20.11.7",
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "hardhat": "^2.22.3"
  }
}
